{
  "Erp": {
    "Access": {
      "_comment": "Development only. The signing key is in a file here so the thing runs out of the box; anywhere real it belongs in user secrets or an environment variable, because whoever holds it can mint a token for anybody with any permission. The bootstrap administrator is created once, on an empty users table, and must change this password on first sign-in.",
      "SigningKey": "development-only-signing-key-change-me-32+",
      "BootstrapAdminEmail": "admin@autopecas.local",
      "BootstrapAdminPassword": "change-this-password"
    }
  },
  "Serilog": {
    "MinimumLevel": {
      "Default": "Debug",
      "Override": {
        "Microsoft.AspNetCore": "Information",
        "Microsoft.EntityFrameworkCore.Database.Command": "Information"
      }
    }
  }
}
